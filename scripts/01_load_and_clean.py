# Import necessary libraries
# Load the raw CSV data from the data folder
# Convert the date from integer format to datetime
# Create a new column for attendance rate = Present / Enrolled
# Create a new column for absence rate = Absent / Enrolled
# Drop rows with invalid or missing values (e.g., Enrolled == 0)
# Sort the data by Date and School DBN for time-series consistency
# Save the cleaned data to outputs/cleaned_data.csv
